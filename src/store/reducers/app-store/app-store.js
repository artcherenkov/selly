// import { ActionType } from '../../action';

const initialState = {};

const appStore = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export { appStore };
